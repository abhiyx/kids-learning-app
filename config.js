// Configuration file for difficulty and settings
// Adjust these values to control the difficulty level globally

const CONFIG = {
    // Number range for all exercises
    NUMBER_RANGE_MIN: 1,
    NUMBER_RANGE_MAX: 50,
    
    // Number of answer options (always 3)
    OPTIONS_COUNT: 3,
    
    // Addition and subtraction limits
    // Parents can start with 9 and gradually increase
    MAX_SUM: 9,              // Maximum sum for addition questions (e.g., 3 + 5 = 8, where 8 ≤ 9)
    MAX_DIFFERENCE: 9         // Maximum difference for subtraction questions (e.g., 8 - 3 = 5, where 5 ≤ 9)
};

